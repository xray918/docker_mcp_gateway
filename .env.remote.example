# 远程部署配置
# 复制此文件为 .env.remote 并修改配置

# SSH 连接配置
REMOTE_USER=root
REMOTE_HOST=192.168.1.100
REMOTE_PORT=22
REMOTE_PASSWORD=                # SSH 密码（留空则使用密钥认证）

# 远程项目路径
REMOTE_PATH=/root/docker_mcp_gateway

# 部署模式: git / rsync
# git   - 服务器从 GitHub 拉取代码（推荐）
# rsync - 直接从本地同步代码到服务器
DEPLOY_MODE=git
